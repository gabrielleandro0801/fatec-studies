#include <stdio.h>
#include <stdlib.h>

int main() {
    // EXERCICIO 1.3 PDF
    double A;
    double B;
    double media;

    scanf("%lf %lf", &A, &B);
    media = (A+B)/2;
    printf("Media = %.2lf\n", media);

    return 0;
}
